.top-bar
  %header
    %nav
      = link_to '', '#', id:'logo'
      %ul
        - if user_signed_in?
          %li= link_to 'All Balances', addresses_path, class:'current'
          %li= link_to 'Recent Transactions', '#'
          %li= link_to 'Log out', destroy_user_session_path, method: :delete
        - else
          %li= link_to 'Sign up', new_user_registration_path
          %li= link_to 'Sign in', new_user_session_path
    .user
      = link_to 'wellsosaurus ▾', '#'
      %ul.account-menu
        %li= link_to 'Account Settings', '#'
        %li= link_to 'Feedback', '#'
        - if user_signed_in?
          %li= link_to 'Log out', destroy_user_session_path, method: :delete

